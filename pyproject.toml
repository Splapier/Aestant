[project]
name = "aestant"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.1.1",
    "gradio[mcp]>=6.9.0",
    "pyyaml>=6.0.0",
    "requests>=2.31.0",
    "pytest>=9.0.2",
    "pytest-playwright>=0.7.2",
    "transformers>=4.37.0",
    "torch>=2.1.0",
]

[tool.pytest.ini_options]
pythonpath = ["tests/playwright"]
